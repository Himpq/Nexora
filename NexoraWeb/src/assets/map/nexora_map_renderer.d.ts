/**
 * nexora_map_renderer.js 的类型占位。
 *
 * 渲染器本体是视觉冻结的自洽 IIFE:无任何导出,副作用为挂载
 * window.NexoraMapRenderer 并安装 MutationObserver(见 mapRenderer.ts 头注)。
 * 此声明仅让工程内 `import('...nexora_map_renderer.js')` 通过类型检查,
 * 禁止向本文件添加运行时代码。
 */

export {}
